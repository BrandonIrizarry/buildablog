<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/static/styles.css">
        <title>Archives</title>
    </head>
    <body>
        {{ template "nav.html" . }}
        <main>
            <h1 class="page-title">Archives</h1>
            <section>
                {{ $tagFilter := .Tag }}
                {{ range .Published }}
                    {{ if (or (eq $tagFilter "") (hasTag $tagFilter .Tags)) }}
                        <details class="published">
                            <summary>
                                <span class="created">{{ (humanReadable .Created) }}: </span>
                                <a href="posts/{{ .Slug }}">{{ .Title }}</a>
                            </summary>

                            <!-- Content of the details element. -->
                            <div>
                                <p class="summary">{{ .Summary }} <a href="posts/{{ .Slug }}">(more)</a></p>
                                {{  if (gt (len .Tags) 0) }}
                                    {{ $numTags := (len .Tags) }}
                                    <div>
                                        <span class="meta">Tags: </span>

                                        {{ range $i, $tag := .Tags }}
                                            <a href="/archives?tag={{ $tag }}"><span class="tags">{{ $tag }}</span></a>{{ if (lt $i (dec $numTags)) }}<span class="meta">,</span>{{ end }}
                                        {{ end }}
                                    </div>
                                {{ end }}
                                {{ if (gt .Updated .Created) }}
                                    <br>
                                    <p><span class="updated">Updated on {{ (humanReadable .Updated) }}</span></p>
                                {{ end }}
                            </div>
                        </details>
                    {{ end }}
                {{ end }}
            </section>
            {{ if (ne $tagFilter "") }}
                <a href="/archives"><span>Show full archives</span></a>
            {{ end }}
        </main>
    </body>
</html>
