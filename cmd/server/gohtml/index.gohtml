{{ define "index" }}

    <!DOCTYPE html>
    <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <link rel="stylesheet" href="/static/styles.css">
            <!-- By convention, the first element of the Posts slice is the host page's own material,
                 and the rest of the slice comprises any listing of posts to be included (either in their entirety or else in
                 summary form.) -->
            <title>{{ .Intro.Frontmatter.Title }}</title>
        </head>
        <body>
            {{ template "nav" . }}
            <main>
                <h1 class="page-title">{{ .Intro.Frontmatter.Title }}</h1>
                <div class="post-content">
                    {{ .Intro.Content }}
                </div>
                <section>
                    <h2> Recent posts: </h2>
                    {{ template "post-listing" (slice .Posts 0 (min (len .Posts) 3)) }}
                </section>
                <section>
                    <h2> Recent projects: </h2>
                    {{ template "project-listing" (slice .Projects 0 (min (len .Projects) 3)) }}
                </section>
            </main>
            {{ template "footer" . }}
        </body>
    </html>

{{ end }}
